{% extends 'baseback.html.twig' %}

{% block body %}
  {% include 'sidebar.twig' %}
  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <!-- Main content -->
    <section class="content">
      <a href="{{ path('app_statistics') }}" class="btn btn-green mb-1 mt-1">
    <i class="fas fa-chart-bar"></i> Afficher Statistique
</a>

<form action="{{ path('app_reclamation_recherche') }}" method="get" class="mb-3">
    <div class="input-group">
        <input type="text" name="q" class="form-control" placeholder="Recherche par titre..." aria-label="Recherche" aria-describedby="button-addon2">
        <div class="input-group-append">
            <button class="btn btn-outline-secondary" type="submit" id="button-addon2">Rechercher</button>
        </div>
    </div>
</form>

<!-- Default box -->
                <div class="card bg-light">
                    <div class="card-header text-white bg-blue border-bottom-0">
                        Reclamation
                    </div>
                      <table class="table table-striped-columns">
                        <thead>
                          <tr>
                            <th>Nom</th>
                            <th>Email</th>
                            <th>Objet</th>
                            <th>Contenu</th>
                            <th>Date & heure</th>
                            <th>Image</th>
                            <th>Actions</th>
                            <th>Réponse</th>
                          </tr>
                        </thead>
                        <tbody>
                        {% for reclamation in reclamationsf %}
                          <tr>
                            <td>{{reclamation.nom}}</td>
                            <td>{{reclamation.email}}</td>
                            <td>{{reclamation.obj}}</td>
                            <td>{{reclamation.contenu}}</td>
                            <td>{{reclamation.type}}</td>
                            <td>
                            <div class="col-6 text-center">
                                {% if reclamation.image %}
                                   <img src="{{ asset('imgrec/' ~ reclamation.image) }}" alt="user-avatar" class="img-fluid mx-auto" style="width: 120px; height: 80px;">
                                {% endif %}
                            </div>
                            </td>
                            <td><a href="{{ path('app_reponse_new', {'id': reclamation.id}) }}" style ="color:green;">Repondre </a>|<a href="" style ="color:red;"> Fermer</a>
                            </td>
                            <td>
                            {% if reclamation.etat == 1 %}
                            <a href="{{ path('app_reponse_show', {'id': reclamation.id}) }}" style ="color:black;" class="btn btn-warning">Afficher Réponse</a>
                            {% else %}
                            {% endif %}
                            </td>
                          </tr>
                          {% endfor %}
                        </tbody>  
                      </table>
      </div>
      <!-- /.card -->
    </section>
  </div>
  <!-- /.content-wrapper -->

  <footer class="main-footer">
    <div class="float-right d-none d-sm-block">
      <b>Version</b> 3.1.0
    </div>
    <strong>Copyright &copy; 2014-2021 <a href="https://adminlte.io">AdminLTE.io</a>.</strong> All rights reserved.
  </footer>

  <!-- Control Sidebar -->
  <aside class="control-sidebar control-sidebar-dark">
    <!-- Control sidebar content goes here -->
  </aside>
  <!-- /.control-sidebar -->
</div>
<!-- ./wrapper -->
{% endblock %}


